# Python Random Password Gen

its just the python version of the C# password generator.
you run it in python, this does not require you to install any dependencies, it only uses pythons standard library.
